import React from 'react';
import {Text, View} from 'react-native';

const StockOffline = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>All StockOffline! we need to test the online mode before</Text>
    </View>
  );
};
export default StockOffline;